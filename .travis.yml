language:
    - python
# python version
python:
  - 3.5
# install dependencies
install:
    - pip install -r requirements.txt --use-mirrors
# run tests
script:
    - python3 runtests.py
sudo:
    false
